# Prompts to test models in Pi



## 1

Can you create a function in F# where the given text is transformed and all the numbers are replaced by random numbers?


## 2

`dotnet workload install maui-android 2>&1 | tail -5`
2>&1 means send theerror to ;standrd ouput, right?
Why tail is xecuted the redirction, shuld not be exece before ( dotnet ... | tail -5 | 2>&1 )? 